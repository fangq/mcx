create table mcxcloud(
time integer primary key, name varchar(32), inst varchar(64), email varchar(64), netname varchar(32), json varchar(1024), shape varchar(1024), jobid varchar(32), status integer, priority float, starttime integer, endtime integer, server varchar(32)
);


